# Core API + vector store + local embeddings (CPU only, no GPU needed)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0
PyYAML>=6.0
qdrant-client>=1.12,<2.0
fastembed>=0.5,<1.0
slowapi>=0.1.9

# MCP client (the portable connector)
mcp>=1.2
httpx>=0.27
